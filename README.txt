Rodar no terminal:
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py


Abrir a aplicação:

se n abrir:
No Codespaces, vá até a aba PORTS
A porta 5000 vai estar com uma URL parecida com:
https://scaling-space-meme-r45w57rvgjjx244q-5000.app.github.dev/
Abre o link e se n rodar por causa do database adiciona isso aqui no final do url: /init-db
